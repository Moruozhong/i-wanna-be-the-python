# 水功能测试指南

## 快速开始

### 方法1：直接启动游戏（推荐）
```bash
python main.py
```
默认进入 room001——水在第 7 行的高空（平台带上方），跳进去即可体验三种水。

### 方法2：使用命令行参数（水专用演示房）
```bash
python main.py --water-test
```
加载 `rooms/water_test_room.json`：水池结构，一段/二段水在第 16 行、零段水在第 14 行。

### 方法3：运行测试脚本
```bash
python test_water.py          # 手动试玩（--water-test 同房）
python tests/room_audit.py    # 无头审计全部房间（出生安全/出口/存档点）
```

## 房间布局（以实际 JSON 为准）

> 注：`rooms/*.json` 优先于内置 `levels/sample_room.py`；以下位置全部对应 JSON 实际数据。

### room001（默认房间）
**水在第 7 行高空**（y=224..256），下方第 8 行有单向平台带（x=352..704）接住，跳上平台即泡在水里：

| 类型 | 位置（网格坐标） | 特性 |
|---|---|---|
| 一段水（浅蓝） | (3,7) (4,7) (5,7) | 减慢下落；**不刷新跳跃次数** |
| 二段水（蓝） | (16,7) (17,7) (18,7) | 减慢下落；**一进入就重置跳跃次数** |
| 零段水（浅灰） | (21,7) (22,7) (23,7) | 减慢下落；**禁止跳跃**（保留跳跃次数） |

起点 (200,352) 会落到第 12 行的单向平台带上（x=192..384），先熟悉平台手感，再沿右侧上行体验水。

### room002（第二个房间）
**水在第 5 行高空**（y=160..192），接近第 10 行平台带（x=128..288 和 512..672）：

| 类型 | 位置（网格坐标） |
|---|---|
| 一段水 | (3,5) (4,5) |
| 二段水 | (18,5) (19,5) |
| 零段水 | (22,5) (23,5) |

另有：两颗跳跃星星（三段星 (5,16)、一段星 (18,15)）、左侧藤蔓 (14,15/16)、终点 (22,16)、出口 (2,16)→room001、可选门 (19,14)→room003。

### water_test_room（--water-test 专用演示房）
水池结构：第 17 行地板在 6-18 列断开（坑深 1 格，掉进坑里落回第 18 行地板），水悬在坑上方，跳起来穿过水面体验：

| 类型 | 位置（网格坐标） |
|---|---|
| 一段水（浅蓝） | (6,16)..(9,16) |
| 二段水（蓝） | (15,16)..(18,16) |
| 零段水（浅灰） | (6,14)..(9,14) 和 (15,14)..(18,14) |

### mini_spike_test_room（小刺演示房）
第 17 行空气层铺了 16 个小刺（4 方向 × 4 象限），落地即死；出口 (24,16)→room001。

### room003（星空试炼，可选挑战房）
从 room002 的门 (19,14) 进入。全地板走廊：一段跳跃星（三段跳）、左侧藤蔓、跨过三种水的平台带、跳跃球；出口 (0,16)→room001、(24,16)→room002。

## 测试步骤

1. **启动游戏**（方法 1 或 2）

2. **测试一段水（浅蓝色）**
   - 跳进浅蓝水域，观察下落速度变慢（最大 2.4）
   - 尝试跳跃，跳跃次数**不会**被刷新

3. **测试二段水（蓝色）**
   - 进入蓝色水域，跳跃次数立即重置为 0
   - 水中跳跃**不消耗**次数，接触地面会刷新 → 可以连续跳（泳池无限跳）
   - **离开水面（空中）只剩一次跳**：一段/二段水出水都视为已用一段跳，空中最多再二段跳那一下，不会白得满额二段跳（零段水出水保留次数）

4. **测试零段水（浅灰色）**
   - 进入浅灰水域，**不能跳跃**，但跳跃次数被保留（离开后可跳）

5. **测试对比**
   - 在同一片区域对比三种水的差异
   - 按 F1 显示碰撞盒，确认水区域边界

## 控制说明

- **方向键**：移动角色
- **Shift**：跳跃（一段/二段/三段，视跳跃星星而定）
- **Z**：射击
- **S**：存档（在有 checkpoint 的地方）
- **R**：重新开始
- **F1**：显示碰撞盒
- **F2**：隐藏参数面板（独立小窗口）

## 预期效果

1. **视觉上**：三种水都是半透明色块——浅蓝（一段）、蓝（二段）、浅灰（零段），且渲染在玩家角色之上（角色被水覆盖）。
2. **物理上**：所有水都把最大下落速度压到 `WATER_FALL_SPEED=2.4`；水平移动正常。
3. **跳跃机制**：水中跳跃不消耗次数（所有水）；**一段水/二段水离开水面（空中）补记一段，只剩一次跳**（不会白得满额二段跳）；一段水不刷新、二段水进入/接触地面刷新（泳池无限跳）；零段水禁止跳跃但保留次数（出水不补记）。

## 故障排除

如果看不到水：
1. 确认运行的是 room001（`python main.py`）或 `--water-test`
2. 检查 `assets/objects/water_*.png` 是否存在（缺失会用半透明占位图）
3. 按 F1 查看碰撞盒，确认水区域存在

如果物理不正常：
1. 确认角色**碰撞箱**与水域重叠（站在水正下方的平台上）
2. 检查控制台是否有错误信息
3. 尝试重新运行程序
